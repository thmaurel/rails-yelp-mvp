<div class="container text-center">
  <h1>Restaurants :</h1>
  <% @restaurants.each do |restaurant| %>
    <p><%= link_to restaurant.name, restaurant_path(restaurant) %></p>
  <% end %>

  <%= link_to "+Add a new restaurant", restaurants_new_path, class:"btn btn-primary" %>
</div>
